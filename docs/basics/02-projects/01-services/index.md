# Services

Services represent actual processes that run for your application. Most web applications will be comprised of a `web` service which contains a port, and a `background_worker` service, which does not have a port.