# Cron Jobs

Cron jobs are run on a schedule, and the success and failure is tracked by Kubernetes. Canine uses the Kubernetes API to help you monitor the success / failure of the jobs that are created by the `cron_job` service.